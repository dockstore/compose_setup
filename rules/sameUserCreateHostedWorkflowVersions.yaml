# (Required)
# Rule name, must be unique
name: Same User Create Many Hosted Workflow Versions Rule

# (Required)
# Type of alert.
# the frequency rule type alerts when num_events events occur with timeframe time
type: frequency

# (Required)
# Index to search, wildcard supported
index: logstash-*

# (Required, frequency specific)
# Alert when this many documents matching the query occur within a timeframe
num_events: 10

# (Required, frequency specific)
# num_events must occur within this amount of time to trigger an alert
timeframe:
  hours: 1

query_key: clientip
# (Required)
# A list of Elasticsearch filters used for find events
# These filters are joined with AND and nested in a filtered query
# For more info: http://www.elasticsearch.org/guide/en/elasticsearch/reference/current/query-dsl.html
filter:
- term:
    verb.keyword: "PATCH"

alert_subject: "User Created Many Hosted Workflow Versions"

alert_text_type: alert_text_only
alert_text: "{} created more than 10 hosted workflows Versions"
alert_text_args:
  - clientip
# The alert is use when a match is found
alert:
  - slack
slack_webhook_url: "https://hooks.slack.com/services/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"
slack_username_override: "ElastAlert"
